{% comment %} 
      Subscription Section
      Newsletter signup with email input and button
      Similar to Feel the Difference but with newsletter form instead of icon+text 
{% endcomment %}

{{ 'subscription.css' | asset_url | stylesheet_tag }}

<style>
  /* Dynamic Padding - Outer Container */
  .section-{{ section.id }}-padding {
    padding-top: {{ section.settings.paddingtop | round: 0 }}px;
    padding-bottom: {{ section.settings.paddingbottom | round: 0 }}px;
    padding-left: {{ section.settings.paddingleft | round: 0 }}px;
    padding-right: {{ section.settings.paddingright | round: 0 }}px;
  }

  /* Mobile Padding - Outer Container */
  @media screen and (max-width: 768px) {
    .section-{{ section.id }}-padding {
      padding-top: {{ section.settings.paddingtopmobile | round: 0 }}px;
      padding-bottom: {{ section.settings.paddingbottommobile | round: 0 }}px;
      padding-left: {{ section.settings.paddingleftmobile | round: 0 }}px;
      padding-right: {{ section.settings.paddingrightmobile | round: 0 }}px;
    }
  }

  /* Image Container Height - Desktop */
  {% for block in section.blocks %}
    {% if block.type == 'image' %}
      .section-{{ section.id }}
      .subscription-win-image-container {
        height: {{ block.settings.image_height_desktop | round: 0 }}px;
        max-height: {{ block.settings.image_max_height_desktop | round: 0 }}px;
      }

      /* Image Container Height - Mobile */
      @media screen and (max-width: 768px) {
        .section-{{ section.id }} .subscription-win-image-container {
          height: {{ block.settings.image_height_mobile | round: 0 }}px;
          max-height: {{ block.settings.image_max_height_mobile | round: 0 }}px;
        }
      }
    {% endif %}
  {% endfor %}

  /* Content Container Padding - Desktop */
  .section-{{ section.id }} .subscription-win-content {
    padding-top: {{ section.settings.content_paddingtop | round: 0 }}px;
    padding-bottom: {{ section.settings.content_paddingbottom | round: 0 }}px;
    padding-left: {{ section.settings.content_paddingleft | round: 0 }}px;
    padding-right: {{ section.settings.content_paddingright | round: 0 }}px;
  }

  /* Content Container Padding - Mobile */
  @media screen and (max-width: 768px) {
    .section-{{ section.id }} .subscription-win-content {
      padding-top: {{ section.settings.content_paddingtopmobile | round: 0 }}px;
      padding-bottom: {{ section.settings.content_paddingbottommobile | round: 0 }}px;
      padding-left: {{ section.settings.content_paddingleftmobile | round: 0 }}px;
      padding-right: {{ section.settings.content_paddingrightmobile | round: 0 }}px;
    }
  }
</style>

<section class="subscription-win section-{{ section.id }}-padding section-{{ section.id }}">
  <div class="subscription-win-wrapper">

    <!-- Image Container -->
    <div class="subscription-win-image-container">
      {% for block in section.blocks %}
        {% if block.type == 'image' %}
          <!-- Desktop Image -->
          {% if block.settings.image_desktop != blank %}
            {{ block.settings.image_desktop | image_url: width: 1920 | image_tag: class: 'subscription-win-background-image none-480', loading: 'lazy' }}
          {% else %}
            <div class="subscription-win-image-placeholder none-480">
              {{ 'lifestyle-2' | placeholder_svg_tag: 'placeholder-svg' }}
            </div>
          {% endif %}

          <!-- Mobile Image -->
          {% if block.settings.image_mobile != blank %}
            {{ block.settings.image_mobile | image_url: width: 1344 | image_tag: class: 'subscription-win-background-image block-480', loading: 'lazy' }}
          {% elsif block.settings.image_desktop != blank %}
            {{ block.settings.image_desktop | image_url: width: 1344 | image_tag: class: 'subscription-win-background-image block-480', loading: 'lazy' }}
          {% else %}
            <div class="subscription-win-image-placeholder block-480">
              {{ 'lifestyle-2' | placeholder_svg_tag: 'placeholder-svg' }}
            </div>
          {% endif %}
        {% endif %}
      {% endfor %}
    </div>

    <!-- Content Container -->
    <div class="subscription-win-content">

      {% for block in section.blocks %}

        {% if block.type == 'title' %}
          <div class="subscription-win-title-wrapper align-{{ block.settings.alignment }}" {{ block.shopify_attributes }}>
            {% if block.settings.title != blank %}
              <h2 class="subscription-win-title">{{ block.settings.title }}</h2>
            {% endif %}
          </div>
        {% endif %}

        {% if block.type == 'text' %}
          <div class="subscription-win-text-wrapper align-{{ block.settings.alignment }}" {{ block.shopify_attributes }}>
            {% if block.settings.content != blank %}
              <div class="subscription-win-text">{{ block.settings.content }}</div>
            {% endif %}
          </div>
        {% endif %}

        {% if block.type == 'newsletter' %}
          <div class="subscription-win-newsletter-wrapper" {{ block.shopify_attributes }}>
            <form id="SubscriptionWinForm" class="subscription-win-form">
              <div class="subscription-win-form-group">
                <input
                  type="email"
                  id="subscription-win-email"
                  class="subscription-win-input"
                  placeholder="{{ block.settings.placeholder | default: 'Enter your email' }}"
                  required
                  autocomplete="email"
                >
                
                <button type="submit" class="btn--primary subscription-win-button">
                  <span>{{ block.settings.button_text | default: 'Subscribe' }}</span>
                  <svg
                    width="18"
                    height="14"
                    viewBox="0 0 18 14"
                    fill="none"
                    xmlns="http://www.w3.org/2000/svg"
                    class="button-arrow"
                  >
                    <path d="M11.0391 0L9.96094 1.07812L14.8828 6H0V7.5H14.8828L9.96094 12.4219L11.0391 13.5L18 6.75L11.0391 0Z" fill="currentColor" />
                  </svg>
                </button>
              </div>
              <p id="subscription-win-message" class="subscription-win-message" style="display:none;"></p>
            </form>
          </div>
          
          <script>
            document.getElementById('SubscriptionWinForm').addEventListener('submit', async function(e) {
              e.preventDefault();

              const email = document.getElementById('subscription-win-email').value.trim();
              const messageElem = document.getElementById('subscription-win-message');

              messageElem.style.display = "none";
              messageElem.textContent = "";

              // Build URL-encoded form data
              const formData = new URLSearchParams();
              formData.append("g", "VNYwiE"); // Your List ID
              formData.append("email", email);

              // OPTIONAL fields (remove if not needed)
              // formData.append("$fields", "$source");
              // formData.append("$source", "Shopify Custom Form");

              try {
                const res = await fetch("https://manage.kmail-lists.com/ajax/subscriptions/subscribe", {
                  method: "POST",
                  headers: {
                    "Content-Type": "application/x-www-form-urlencoded"
                  },
                  body: formData.toString()
                });

                const data = await res.json();

                if (data.success) {
                  messageElem.textContent = "{{ block.settings.success_message | default: 'Thanks for subscribing!' }}";
                  messageElem.style.color = "green";
                  messageElem.style.display = "block";
                  document.getElementById('subscription-win-email').value = "";
                } else {
                  messageElem.textContent = data.message || "Subscription failed.";
                  messageElem.style.color = "red";
                  messageElem.style.display = "block";
                }

              } catch (err) {
                messageElem.textContent = "Something went wrong. Try again.";
                messageElem.style.color = "red";
                messageElem.style.display = "block";
              }
            });
          </script>
        {% endif %}

      {% endfor %}

    </div>

  </div>
</section>

{% schema %}
  {
    "name": "Subscription",
    "settings": [
      {
        "type": "header",
        "content": "Section Padding (Desktop)"
      },
      {
        "type": "range",
        "id": "paddingtop",
        "min": 0,
        "max": 100,
        "step": 1,
        "unit": "px",
        "label": "Padding Top",
        "default": 0
      },
      {
        "type": "range",
        "id": "paddingbottom",
        "min": 0,
        "max": 100,
        "step": 1,
        "unit": "px",
        "label": "Padding Bottom",
        "default": 0
      },
      {
        "type": "range",
        "id": "paddingleft",
        "min": 0,
        "max": 100,
        "step": 1,
        "unit": "px",
        "label": "Padding Left",
        "default": 0
      }, {
        "type": "range",
        "id": "paddingright",
        "min": 0,
        "max": 100,
        "step": 1,
        "unit": "px",
        "label": "Padding Right",
        "default": 0
      }, {
        "type": "header",
        "content": "Section Padding (Mobile)"
      }, {
        "type": "range",
        "id": "paddingtopmobile",
        "min": 0,
        "max": 100,
        "step": 1,
        "unit": "px",
        "label": "Padding Top",
        "default": 32
      }, {
        "type": "range",
        "id": "paddingbottommobile",
        "min": 0,
        "max": 100,
        "step": 1,
        "unit": "px",
        "label": "Padding Bottom",
        "default": 0
      }, {
        "type": "range",
        "id": "paddingleftmobile",
        "min": 0,
        "max": 100,
        "step": 1,
        "unit": "px",
        "label": "Padding Left",
        "default": 0
      }, {
        "type": "range",
        "id": "paddingrightmobile",
        "min": 0,
        "max": 100,
        "step": 1,
        "unit": "px",
        "label": "Padding Right",
        "default": 0
      }, {
        "type": "header",
        "content": "Content Padding (Desktop)"
      }, {
        "type": "range",
        "id": "content_paddingtop",
        "min": 0,
        "max": 100,
        "step": 1,
        "unit": "px",
        "label": "Padding Top",
        "default": 40
      }, {
        "type": "range",
        "id": "content_paddingbottom",
        "min": 0,
        "max": 100,
        "step": 1,
        "unit": "px",
        "label": "Padding Bottom",
        "default": 40
      }, {
        "type": "range",
        "id": "content_paddingleft",
        "min": 0,
        "max": 100,
        "step": 1,
        "unit": "px",
        "label": "Padding Left",
        "default": 15
      }, {
        "type": "range",
        "id": "content_paddingright",
        "min": 0,
        "max": 100,
        "step": 1,
        "unit": "px",
        "label": "Padding Right",
        "default": 15
      }, {
        "type": "header",
        "content": "Content Padding (Mobile)"
      }, {
        "type": "range",
        "id": "content_paddingtopmobile",
        "min": 0,
        "max": 100,
        "step": 1,
        "unit": "px",
        "label": "Padding Top",
        "default": 32
      }, {
        "type": "range",
        "id": "content_paddingbottommobile",
        "min": 0,
        "max": 100,
        "step": 1,
        "unit": "px",
        "label": "Padding Bottom",
        "default": 32
      }, {
        "type": "range",
        "id": "content_paddingleftmobile",
        "min": 0,
        "max": 100,
        "step": 1,
        "unit": "px",
        "label": "Padding Left",
        "default": 15
      }, {
        "type": "range",
        "id": "content_paddingrightmobile",
        "min": 0,
        "max": 100,
        "step": 1,
        "unit": "px",
        "label": "Padding Right",
        "default": 15
      }
    ],
    "blocks": [
      {
        "type": "image",
        "name": "Background Image",
        "limit": 1,
        "settings": [
          {
            "type": "image_picker",
            "id": "image_desktop",
            "label": "Image (Desktop)",
            "info": "1920px width recommended"
          },
          {
            "type": "image_picker",
            "id": "image_mobile",
            "label": "Image (Mobile)",
            "info": "1344px width recommended. Falls back to desktop if empty."
          },
          {
            "type": "header",
            "content": "Image Container (Desktop)"
          },
          {
            "type": "range",
            "id": "image_height_desktop",
            "min": 200,
            "max": 800,
            "step": 10,
            "unit": "px",
            "label": "Height",
            "default": 400
          }, {
            "type": "range",
            "id": "image_max_height_desktop",
            "min": 200,
            "max": 800,
            "step": 10,
            "unit": "px",
            "label": "Max Height",
            "default": 600
          }, {
            "type": "header",
            "content": "Image Container (Mobile)"
          }, {
            "type": "range",
            "id": "image_height_mobile",
            "min": 150,
            "max": 600,
            "step": 10,
            "unit": "px",
            "label": "Height",
            "default": 300
          }, {
            "type": "range",
            "id": "image_max_height_mobile",
            "min": 150,
            "max": 600,
            "step": 10,
            "unit": "px",
            "label": "Max Height",
            "default": 400
          }
        ]
      }, {
        "type": "title",
        "name": "Title",
        "limit": 1,
        "settings": [
          {
            "type": "text",
            "id": "title",
            "label": "Title",
            "default": "Subscribe & Win"
          }, {
            "type": "select",
            "id": "alignment",
            "label": "Alignment",
            "options": [
              {
                "value": "left",
                "label": "Left"
              }, {
                "value": "center",
                "label": "Center"
              }, {
                "value": "right",
                "label": "Right"
              }
            ],
            "default": "left"
          }
        ]
      }, {
        "type": "text",
        "name": "Text",
        "limit": 1,
        "settings": [
          {
            "type": "richtext",
            "id": "content",
            "label": "Content",
            "default": "<p>Join our newsletter for exclusive offers and health insights.</p>"
          }, {
            "type": "select",
            "id": "alignment",
            "label": "Alignment",
            "options": [
              {
                "value": "left",
                "label": "Left"
              }, {
                "value": "center",
                "label": "Center"
              }, {
                "value": "right",
                "label": "Right"
              }
            ],
            "default": "left"
          }
        ]
      }, {
        "type": "newsletter",
        "name": "Newsletter Form",
        "limit": 1,
        "settings": [
          {
            "type": "text",
            "id": "placeholder",
            "label": "Email Placeholder",
            "default": "Enter your email"
          }, {
            "type": "text",
            "id": "button_text",
            "label": "Button Text",
            "default": "Subscribe"
          }, {
            "type": "text",
            "id": "success_message",
            "label": "Success Message",
            "default": "Thanks for subscribing!"
          }
        ]
      }
    ],
    "presets": [
      {
        "name": "Subscription",
        "blocks": [
          {
            "type": "image"
          }, {
            "type": "title"
          }, {
            "type": "text"
          }, {
            "type": "newsletter"
          }
        ]
      }
    ]
  }
{% endschema %}